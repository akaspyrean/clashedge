# Intentionally malformed YAML — unclosed bracket + inconsistent indentation.
# This file should fail serde_yaml::from_str parsing, causing
# validate_subscription_content to return "Invalid YAML: ...".
proxies:
  - name: Bad-Node
    type: ss
   server: [1.2.3.4
    port: 8388
    cipher: aes-128-gcm
    password: test-password
