# ClashEdge for Windows

Tauri 2 应用源码（Windows-first，Lightweight / Portable）。**项目总览、目录结构与构建/打包说明见仓库根目录 [`README.md`](../../README.md)。**

```text
src/            # Vue 3 前端（views / stores / api / i18n / router）
src-tauri/src/  # Rust 后端（commands / config / core / proxy / geodata / tray / util）
```
